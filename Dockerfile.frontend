FROM node:20-alpine

WORKDIR /app
COPY package*.json ./
COPY tsconfig*.json ./
COPY vite.config.ts ./
COPY src ./src
COPY index.html ./

RUN npm install
RUN npm run build

# Add a simple static file server
RUN npm install -g serve
EXPOSE 8081
CMD ["serve", "-s", "dist", "-l", "8081"]